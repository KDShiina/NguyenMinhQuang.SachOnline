

@{
    ViewBag.Title = "NavPartial";

}


<nav class="navbar navbar-inverse">
    <!--navbar-fixed-top-->
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data- target="#navbar1" aria-expanded="false">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">SachOnline</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling (Chứa nội dung của navbar) -->
        <div class="collapse navbar-collapse" id="navbar1">
            <ul class="nav navbar-nav">
                <li class="active"><a href="~/SachOnline/Index">TRANG CHỦ</a> </li>
                <li class="active"><a href="#">GIỚI THIỆU</a> </li>
                <li class="active"><a href="#">LIÊN HỆ</a> </li>
            </ul>

            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    @if (Session["TaiKhoan"] != null)
                    {
                        // Hiển thị menu cho người dùng đã đăng nhập
                        <a href="#" id="userDropdown" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" aria-haspopup="true"><span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a href="/User/chiTietKH"><span class="glyphicon glyphicon-user"></span> Thông tin cá nhân</a></li>
                            <li role="separator" class="divider"></li>
                            <li><a href="/User/DangXuat"><span class="glyphicon glyphicon-log-in"></span> Đăng xuất</a></li>
                        </ul>

                        <script>
                             var userName = "@Session["HoTen"]";
                             document.getElementById("userDropdown").innerHTML = "Xin chào: "+ userName + '<span class="caret"></span>';
                        </script>
                    }
                    else
                    {
                        // Hiển thị menu cho người dùng chưa đăng nhập
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" aria-haspopup="true">Người dùng <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a href="#"><span class="glyphicon glyphicon-user"></span> Đăng ký</a> </li>
                            <li role="separator" class="divider"></li>
                            <li><a href="~/User/DangNhap"><span class="glyphicon glyphicon-log-in"></span> Đăng nhập</a></li>
                        </ul>
                    }
<li>
                    <a href="#"><span class="glyphicon glyphicon-shopping-cart"></span> Giỏ hàng (0)</a>
                </li>
            </ul>

            <form class="navbar-form navbar-right" role="search" action="@Url.Action("Search", "SachOnline")" method="GET">
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="Tìm kiếm" name="keyword">
                </div>
                <button type="submit" class="btn btn-default">Tìm</button>
            </form>

        </div>
        <!-- /.navbar-collapse -->

    </div>
    <!-- /.container-->
</nav>


